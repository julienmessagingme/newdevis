# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel build output (regénéré automatiquement — ne pas committer)
.vercel/output/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Env files
.env
.env.*

# SEO agent runtime artifacts (.github/workflows/seo-agent.yml)
.secrets/
report.html
claude-err.log
issue-body.md
.claude/worktrees/

# feature-loop : fichiers d'état de boucle (contrat + progrès), artefact de travail
.loop/

# Claude Code local settings (peut contenir des règles avec secrets)
.claude/settings.local.json

# Fichiers de travail locaux — jamais versionnes (incident 2026-09-05 : un
# `git add -A` les avait embarques dans un commit de correctif).
backup-*.json
costa-surgery.mjs
fm.cjs
.astro/
supabase/.temp/
scratch/
.cache-rejeu/
relecture-rapprochement.csv
relecture-rapprochement.cle.json
relecture-rapprochement.md
relecture-reponses.json
relecture-ia.json
# Feuille de relecture des postes accusés (2026-09-17) — contient des lignes de
# devis de clients réels ET les tarifs d'artisans identifiables ; le dépôt est
# public. Même règle que l'étalon de rapprochement du 10/09.
relecture-postes-jamais-juges.md
relecture-postes*.md
desaccords.json
rattrapage-envois.json
scratchpad/

# Sortie du sourceur de prix IA (2026-09-17) — contient des lignes de devis
# de clients reels. Depot public : meme regle que la feuille de relecture.
sourcage-*.json
rapport-sourcage*.md

# 2026-09-17 — Sauvegardes de conclusion avant rejeu forcé (rejouer-une-analyse.mjs).
# Elles contiennent la conclusion COMPLÈTE d'un devis de client réel — lignes,
# montants, entreprise. Le dépôt est public : même règle que l'étalon du 10/09
# et que les feuilles de relecture.
sauvegarde-conclusion-*.json
